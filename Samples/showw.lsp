(defun showw (w) 
       (prog (k)
             (grwShow w)
    @Inkey   (setq k (grwinkey w))
             (cond ((= k 0) (go @Inkey)))
             (grwHide w)
             (return k) 
       )
)
